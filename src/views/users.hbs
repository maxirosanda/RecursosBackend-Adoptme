<h1>Users List</h1>
<ul>
    {{#if users.length}}
        {{#each users}}
            <li>{{this.first_name}} - {{this.last_name}}</li>
        {{/each}}
    {{else}}
        <li>No users found</li>
    {{/if}}
</ul>
